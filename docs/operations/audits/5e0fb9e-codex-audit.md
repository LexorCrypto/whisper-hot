```text
base: 396fb9a7801b24f51dbd8993f82af6737bf018ac
tip: 5e0fb9e9cffc60f4116491bfb2d41f035142fff0
tree: 1902752e0d758ed7ccd77424176c026ddb9e3582
verdict: ITERATE
rounds: 4
owner-go-ahead: 2026-07-27 — владелец разрешил третий раунд, затем отдельно четвёртый на main
risk: full
model: gpt-5.6-sol   effort: high
```

# Codex-аудит 396fb9a..5e0fb9e — раунд 4 (авторитетный)

**Заменяет раунд 3.** Запись `1d50e19-codex-audit.md` помечена недействительной:
она выполнялась в общем кеш-каталоге, который параллельно перезаписал другой
прогон `close-session`. Этот раунд выполнен в изолированном по проекту каталоге
`$HOME/.cache/close-session-audit-whisper-hot`.

**Изоляция подтверждена измерением:** в `verdict-round4.stdout` 560 упоминаний
`AudioRecorder.swift` и 2 упоминания посторонних файлов (оба — в цитатах наших
же документов). Для сравнения, в скомпрометированном `verdict-round3.stdout` —
0 и 251 соответственно.

Арбитр: `gpt-5.6-sol`, effort high, отдельный `CODEX_HOME`, permission-профиль
`cs-audit` (`extends = ":read-only"`), без MCP. Preflight: диапазон 13 коммитов,
16 файлов.

## Итог четырёх раундов

| Раунд | Находка | Статус |
|---|---|---|
| 1 | [P1] `tapDrainTimedOut` ретраился до 10 раз на main queue — до 5 с блокировки главного потока | закрыт `4bbaa5e`, подтверждено раундом 2 |
| 2 | [P3] SPEC не обновлён вслед за исправлением | закрыт `62a188b`, подтверждено раундом 4 |
| 3 | — | **недействителен**, дерево подменялось |
| 4 | [P3] порядок миграции описан неверно: `sessionLock` очищается до fallible-дренажа | закрыт `5e0fb9e`, **не подтверждён** |

**Дефектов кода уровня [P0]/[P1]/[P2] раунд 4 не нашёл.** Он явно проверял
конкурентность, блокировки главного потока с подсчётом худшего случая, утечки
observer'ов и tap-замыканий, целостность WAV, все три пути отказа, а также
sleep/wake recovery и согласованность версии 0.9.2.

## Почему `verdict: ITERATE`

Раунд 4 завершился `ITERATE` из-за своей [P3]. Исправление внесено **после**
аудита коммитом `5e0fb9e` и **независимым раундом не подтверждено**; пятый
раунд требует отдельного разрешения владельца. Поэтому запись **покрытия не
даёт**, и коммиты `396fb9a..5e0fb9e` остаются непокрытым долгом.

Отдельно: эти коммиты ушли прямо в `main` без PR, поэтому
`audit_coverage.py` даёт `session 0` и структурно их уже не увидит. Владелец
зафиксировал политику «в main только через PR» — `CLAUDE.md`.

## Вердикт раунда 4 — дословно

[P3] `SPEC.md:314,508`, `CHANGELOG.md:69-70` — порядок миграции описан неверно:
`AudioRecorder.swift:485-488` очищает `sessionLock` до fallible-ожидания
`tapGroup`; при таймауте исходная сессия восстанавливается. Поэтому утверждение,
что дренаж выполняется до любого изменения живой сессии, ложно, хотя целостность
WAV не нарушается.

VERDICT: ITERATE

## Провенанс

`$HOME/.cache/close-session-audit-whisper-hot/verdict-round4.stdout` — эфемерный
кеш вне репозитория, целиком не коммитится. Выше — вердикт дословно.
