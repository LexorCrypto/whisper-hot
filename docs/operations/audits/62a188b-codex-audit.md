```text
base: 396fb9a7801b24f51dbd8993f82af6737bf018ac
tip: 62a188b7ffa41bfb51c9c110149adffc4a84cb96
tree: 9780c91561d19f61fc1fb49bc41731bc8e34054b
verdict: ITERATE
rounds: 2
risk: full
model: gpt-5.6-sol   effort: high
```

# Codex-аудит 396fb9a..62a188b — v0.9.1 (миграция аудиоустройства, PRD/SPEC)

Диапазон охватывает фикс смены микрофона (ADR-019), продуктовые документы
`PRD.md` / `SPEC.md`, правки формулировок объёма проверки и две ремедиации
по находкам самого аудита.

Арбитр запускался в изолированном `CODEX_HOME` под permission-профилем
`cs-audit` (`extends = ":read-only"`, чтение ограничено standalone-клоном),
без MCP. Preflight подтвердил, что арбитр видит диапазон.

**Исправление (внесено после раунда 3).** В первой редакции этого абзаца
стояло «`git rev-list --count` = 4, `git diff --stat` = 13 файлов». Эти
числа относятся к диапазону раунда 1 `396fb9a..7e9b4e0`, а не к диапазону
заголовка `396fb9a..62a188b`, в котором коммитов **6**. Ошибка найдена
раундом 3 как `[P3]`; `base`, `tip` и `tree` были и остаются верными.

**Почему `verdict: ITERATE` при закрытых находках.** Раунд 2 завершился
`ITERATE` из-за [P3] (SPEC разошёлся с кодом). Находка исправлена коммитом
`62a188b`, который и является `tip` этой записи, но подтвердить это может
только третий раунд, а он по §3.4 требует явного разрешения владельца.
Запись честно фиксирует состояние на момент окончания второго раунда и
покрытия не даёт.

## Раунд 1 — диапазон 396fb9a..7e9b4e0

99 336
- [P1] `Sources/WhisperHot/Audio/AudioRecorder.swift:413-438,472-475` — `tapDrainTimedOut` повторяется до 10 раз на main queue. При зависшем tap-callback главный поток блокируется суммарно до 5 секунд (`10 × 500 мс`), а весь отказ занимает до 6,35 секунды с ретраями. Это нарушает заявленную неблокирующую обработку зависшего callback; timeout следует считать терминальным либо ожидать drain вне main queue.

VERDICT: ITERATE

## Раунд 2 — диапазон 396fb9a..4bbaa5e (после ремедиации [P1])

- Статус находки раунда 1: закрыта. `tapDrainTimedOut` теперь перехватывается отдельным типизированным `catch` и немедленно ведёт в `abandonSwitch`; повторного входа в drain нет. Новый худший случай блокировки main thread — один `tapGroup.wait(timeout:)`, номинально до 500 мс. Остальные ошибки по-прежнему ретраятся до 10 попыток; `isSwitchingDevice`, generation-гейт, observer lifecycle и порядок WAV-записей сохранены корректно.

- Новые находки:
  - [P3] `SPEC.md:309-310,509,705,989` — спецификация не обновлена вслед за исправлением: она всё ещё требует ретраить любую ошибку до 10 раз, утверждает, что `tapDrainTimedOut` ретраится, и оставляет вопрос №20 открытым. Это прямо противоречит новому терминальному поведению и заявленной цели коммита.

VERDICT: ITERATE

## Ремедиация

- [P1] закрыт коммитом `4bbaa5e` — `tapDrainTimedOut` перехватывается типизированным `catch` и ведёт прямо на `abandonSwitch`; худший случай блокировки главного потока на этом пути — один дренаж 500 мс вместо 5 с. Подтверждено раундом 2.
- [P3] закрыт коммитом `62a188b` — добавлено SR-AUD-019, уточнено SR-AUD-009, поправлены §4.2 и §8.1, закрыты открытые вопросы №20 и №21. Третьим раундом не подтверждалось.
