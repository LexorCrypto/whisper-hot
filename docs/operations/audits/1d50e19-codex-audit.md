```text
base: 396fb9a7801b24f51dbd8993f82af6737bf018ac
tip: 1d50e1951374f0c2b84e51189d5ffa11a2a2c36e
tree: 7a69b861894845cd962a0516f0019dbdf587e8f7
verdict: ITERATE
rounds: 3
owner-go-ahead: 2026-07-27 — владелец явно разрешил третий раунд в ответ на вопрос о покрытии
risk: full
model: gpt-5.6-sol   effort: high
```

# Codex-аудит 396fb9a..1d50e19 — раунд 3

Продолжение записи `62a188b-codex-audit.md` (раунды 1–2), которая остаётся
неизменной как исторический артефакт. Диапазон тот же, расширенный до
текущего tip.

Изоляция та же: отдельный `CODEX_HOME`, permission-профиль `cs-audit`
(`extends = ":read-only"`, чтение ограничено standalone-клоном), без MCP.

## Итог трёх раундов

| Раунд | Диапазон | Находка | Статус |
|---|---|---|---|
| 1 | `396fb9a..7e9b4e0` | [P1] `tapDrainTimedOut` ретраился до 10 раз на main queue — до 5 с блокировки главного потока | закрыт `4bbaa5e`, подтверждено раундом 2 |
| 2 | `396fb9a..4bbaa5e` | [P3] SPEC не обновлён вслед за исправлением | закрыт `62a188b`, подтверждено раундом 3 |
| 3 | `396fb9a..bb8471c` | [P3] SR-AUD-008 и инвариант §4.2 неверно описывали поведение при отказе `engine.start()` | закрыт `f593662`, **третьим лицом не подтверждён** |
| 3 | `396fb9a..bb8471c` | [P3] запись раунда 2 приводила `rev-list --count = 4` для диапазона из 6 коммитов | поправка здесь, см. ниже; сама запись раунда 2 не редактировалась |

**Дефектов кода уровня [P0]/[P1]/[P2] за три раунда не осталось.** Раунд 3
явно проверял конкурентность, взаимоблокировки, lifecycle, утечки и порядок
записей в WAV и не нашёл новых.

## Поправка к записи раунда 2

В `62a188b-codex-audit.md` указано «`git rev-list --count` = 4,
`git diff --stat` = 13 файлов». Эти числа относятся к диапазону раунда 1
`396fb9a..7e9b4e0`, а не к диапазону заголовка той записи
`396fb9a..62a188b`, где коммитов **6**. `base`, `tip` и `tree` там верны.
Сама запись намеренно оставлена без изменений.

## Почему `verdict: ITERATE`

Раунд 3 завершился `ITERATE` из-за двух [P3]. Обе исправлены (`f593662` и
эта запись), но подтвердить исправление может только четвёртый раунд,
которого не было. Запись фиксирует состояние на конец третьего раунда и
**покрытия не даёт** — коммиты диапазона остаются непокрытым долгом.

## Вердикт раунда 3 — дословно

Статус [P3] раунда 2: закрыт. `AudioRecorder.swift:427-432` обрабатывает
`tapDrainTimedOut` отдельным терминальным `catch`. SPEC.md теперь соответствует
этому поведению. Старых утверждений о ретрае `tapDrainTimedOut` в SPEC, PRD,
decisions.md или CHANGELOG не осталось. Версия согласована: 0.9.2, build 24.
Секция CHANGELOG 0.9.1 не приписывает релизу исправление из 0.9.2; после тега
её изменения лишь сузили заявленное ручное покрытие.

Новые находки:

- [P3] `SPEC.md:314,508` — нормативный инвариант всё ещё утверждает, что любая
  неудачная миграция оставляет исходную сессию нетронутой. Фактически
  `AudioRecorder.swift:491-515` публикует successor в `sessionLock` до
  `engine.start()`, а при ошибке запуска намеренно сохраняет successor для
  ретрая. Требование и описание автомата следует уточнить.
- [P3] `docs/operations/audits/62a188b-codex-audit.md:17-20` — для указанного в
  заголовке диапазона `396fb9a..62a188b` заявлено `git rev-list --count = 4`,
  тогда как реально в диапазоне 6 коммитов. Base, tip, tree и число изменённых
  файлов корректны; отказ приписывать покрытие ремедиации без третьего раунда
  сформулирован честно.
- Новых [P0]/[P1]/[P2] дефектов кода в конкурентности, блокировках, lifecycle,
  утечках или порядке WAV-записей не обнаружено.

VERDICT: ITERATE

## Провенанс — и его утрата

Полный stdout трёх прогонов писался в `~/.cache/close-session-audit/`. Этот путь
**захардкожен в глобальном скилле `close-session`** (`SKILL.md:485`,
`BASE="$HOME/.cache/close-session-audit"`) и общий для всех проектов, без
неймспейса по репозиторию.

**Что произошло.** Параллельно шёл close-session другого проекта (аудит самого
репозитория `close-session`, диапазон `f332bd3..f6f5c7a`, ветка
`fix/t2-t4-oserror-and-json`) в том же каталоге. Наблюдаемые следы:

- при подготовке своего аудита эта сессия выполнила `rm -rf` общего `BASE` —
  то есть снесла рабочий каталог чужого прогона;
- чужой прогон затем сделал `reset --hard` в `$BASE/repo`: `origin` там до сих
  пор указывает на whisper-hot, а HEAD стоит на `f6f5c7a` чужого репозитория;
- `verdict-round3.stdout` перезаписан их выводом — mtime 22:36 против
  завершения нашего раунда 3 в ~22:28; в файле сейчас 0 упоминаний
  `AudioRecorder.swift` и 251 упоминание их файлов. `verdict.stdout` и
  `verdict2.stdout` (раунды 1–2) не пострадали: 91 и 106 упоминаний
  `AudioRecorder.swift`, 0 чужих.

**Что это значит для доверия к раунду 3.** Сам вердикт получен корректно:
он был прочитан из stdout процесса в момент завершения, содержательно относился
к whisper-hot, и **обе его находки проверены вручную против реального
репозитория** — `engine.start()` действительно вызывается после публикации
successor (`AudioRecorder.swift:491-517`), а в записи раунда 2 действительно
стояло число коммитов от другого диапазона. Утрачена не истинность вердикта, а
его **воспроизводимость**: сырой stdout раунда 3 больше не существует, и
процитированный выше текст — транскрипция, сделанная в момент завершения, а не
файл, который можно перечитать.

**Дефект инструмента, а не проекта.** Общий фиксированный кеш-каталог делает два
одновременных close-session взаимно разрушительными в обе стороны. Каталог
намеренно не тронут после обнаружения — он оставлен владельцу другого прогона.
