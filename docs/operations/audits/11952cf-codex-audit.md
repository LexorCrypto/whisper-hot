```text
base: 5ca71c278ec5e8eebec9b3b4d1a8e6af1cea686e
tip: 11952cf5f7680e0502d7488aa4e05cb1dbb2b15c
tree: 1f410ad4b5c9b35028a50652454bd1c4b7dce9af
verdict: SHIP
rounds: 3
owner-go-ahead: 2026-07-28 — владелец разрешил третий раунд по этому диапазону сверх лимита в 2, чтобы закрыть две находки [P3] раунда 6 и довести ветку до реального покрытия, а не до мержа с named debt
risk: full
model: gpt-5.6-sol   effort: high
```

# Codex-аудит 5ca71c2..11952cf — раунды 5–7 (ветка `fix/swarm-audit-remediation`, PR #18)

**Заменяет запись `d14b363-codex-audit.md`**, которая покрывала промежуточный tip и
закрылась на `ITERATE` (по §3.2 покрытием не является). Та запись остаётся в истории как
честный след раунда 6; авторитетна эта.

**Это первый диапазон проекта, покрытый гейтом, а не мимо него.** Четыре предыдущих
раунда шли прямым push в `main`, где `merge-base HEAD origin/main` совпадает с `HEAD`,
`audit_coverage.py` показывает `session 0` и записи не считаются вообще. Здесь диапазон
живёт в feature-ветке под pull request, поэтому session-набор непустой и запись даёт
настоящее покрытие.

Нумерация раундов сквозная по проекту (1–4 были на `main`); по **этому диапазону** —
три, что и стоит в заголовке.

## Изоляция песочницы — измерена, а не заявлена

Изолированный по проекту каталог `$HOME/.cache/close-session-audit-whisper-hot`, а не
общий `close-session-audit`: в прошлой сессии общий каталог затёр клон параллельным
прогоном другого проекта и обесценил раунд 3 целиком. Standalone-клон (`.git` — реальный
каталог внутри выданного дерева, иначе `git` в linked worktree упирается в `not a git
repository`), отдельный `CODEX_HOME`, permission-профиль `cs-audit`
(`extends = ":read-only"`), `GIT_CONFIG_NOSYSTEM=1` и пустой `GIT_CONFIG_GLOBAL` внутри
выданного дерева.

Баннер всех трёх прогонов: `model: gpt-5.6-sol`, `sandbox: read-only`,
`reasoning effort: high`, **ноль** MCP-строк.

| Прогон | Диапазон | `AudioRecorder.swift` | тест | посторонние проекты |
|---|---|---|---|---|
| раунд 5 | `5ca71c2..8ed3633` | 394 | 40 | 0 |
| раунд 6 | `5ca71c2..d14b363` | 309 | 50 | 0 |
| раунд 7 | `5ca71c2..11952cf` | 249 | 40 | 0 (2 текстовых совпадения — сам арбитр читал diff этой записи, где эти слова перечислены) |

Проверенные маркеры: `calorio`, `kvik`, `paperclip`, `trading-system`, `stablix`,
`hermes`. Для сравнения, у скомпрометированного раунда 3 было 0 упоминаний своих файлов
и 251 постороннего.

Preflight пройден до прогонов: арбитр сам перечислял диапазон изнутри песочницы
(`git rev-list --count`, `git diff --stat`). Арбитр, который не может перечислить
диапазон, диапазон не проверял.

Полный stdout сохранён вне волатильного кеша, в `~/Library/Logs/whisper-hot-audits/`:
`verdict-round5.stdout` (1 084 299 Б), `verdict-round6.stdout` (927 368 Б),
`verdict-round7.stdout` (688 278 Б).

## Ход раундов

| Раунд | Диапазон | Находка | Итог |
|---|---|---|---|
| 5 | `..8ed3633` | **[P2]** `AudioDeviceSwitchSmokeTests.swift:96` — правка `8ed3633` заменила `XCTAssertEqual(status, noErr, …)` безусловным `guard`, из-за чего **любая** ошибка `AudioObjectGetPropertyData` давала зелёный skip обоих тестов | закрыта `d14b363`, подтверждено раундами 6 и 7 |
| 5 | `..8ed3633` | **[P3]** `SPEC.md:900` — «версии до `0.4.0` содержат только "Для пользователей"» неверно | закрыта `d14b363`, подтверждено раундами 6 и 7 |
| 6 | `..d14b363` | **[P3]** `AudioDeviceSwitchSmokeTests.swift:100` — `AudioHardwareBase.h:136-137` не подтверждает валидность `kAudioObjectSystemObject` | закрыта `11952cf`, подтверждено раундом 7 |
| 6 | `..d14b363` | **[P3]** `SPEC.md:914`/`957` — `ContextRouter.resolve` в 3 из 15 тестов, не в 2; helper в 12, не в 13 | закрыта `11952cf`, подтверждено раундом 7 |
| 6 | `..d14b363` | **[P2]** остаток: `inputDevices()`/`inputChannelCount()` глотают `OSStatus` | **предшествует базе** `5ca71c2` (подтверждено самим арбитром и `git show 5ca71c2:…`) → вынесен в issue #19, в этот PR не тащится |
| 7 | `..11952cf` | — | **новых находок нет** |

Каждая находка каждого раунда перепроверялась вручную против репозитория до правки —
вердикт арбитра тоже данные. Все подтвердились.

## Вердикт раунда 7 (дословно)

1. **[P3] ссылка на SDK-заголовок — ЗАКРЫТА.**
   В `AudioDeviceSwitchSmokeTests.swift:100-103` ссылки разделены корректно:

   - установленный SDK `AudioHardware.h:104-106` действительно определяет `kAudioObjectSystemObject` как постоянный ID единственного `AudioSystemObject`;
   - `AudioHardwareBase.h:136-137` определяет `kAudioHardwareBadObjectError` как ID, не соответствующий валидному `AudioObject`.

   `requireDefaultInputDeviceID()` на строках 109–123 бросает ошибку при любом ненулевом `OSStatus`; skip остаётся только для успешного `noErr + kAudioObjectUnknown` на строках 21–24 и 49–52.

2. **[P3] счётчики ContextRouter — ЗАКРЫТА.**
   Самостоятельный пересчёт `ContextRouterTests.swift`:

   - всего 15 тестов;
   - production `ContextRouter.resolve` вызывается в 3 тестах: строки 11, 22–25 и 103;
   - все три вызова получают `target: nil`;
   - в `testSlackMatchesCasual` результат действительно отбрасывается;
   - helper `resolveByBundleID` вызывается в 12 тестах;
   - реальный AX/title-путь не вызывается, title-тест проверяет только поля модели.

   Формулировки и числа в `SPEC.md:914` и `SPEC.md:957` теперь полностью соответствуют тестам.

Новые находки: **нет**.

Повторно проверены:

- все четыре коммита и полный diff диапазона;
- весь `AudioDeviceSwitchSmokeTests.swift`, включая известное проглатывание ошибок в `inputDevices()`/`inputChannelCount()`, существовавшее уже в базе;
- весь `AudioRecorder.swift`: синхронизация сессии, main-thread waits, generation guard, трёхфазная миграция, наследование одной writer-очереди, дренаж tap, observer cleanup, retry/give-up, целостность WAV и orphan-retention;
- все изменённые утверждения в `ARCHITECTURE.md`, `SPEC.md`, `PRD.md` и `CLAUDE.md`, включая версии, 45 Swift-файлов/9294 строки, 56 тестов/6 файлов, 135 требований/12 доменов, backoff 1,35 с и политики retention;
- audit-запись: `base` и `tip` существуют, `tree: c4bee3c91539fce76847265c61653a300790146b` точно равен дереву `d14b363`; описание изменений `8ed3633`/`d14b363` соответствует diff. Внешние stdout-журналы, их размеры и баннеры недоступны read-only песочнице и независимо не подтверждались;
- `git diff --check` — чисто. Build/test не запускались, поскольку это нарушило бы строгий read-only режим.

VERDICT: SHIP

## Границы того, что эта запись доказывает

Запись — файл в репозитории, поэтому она подтверждает **топологию и целостность**
(диапазон резолвится, `tree` равен дереву `tip`, коммиты внутри диапазона), но **не
провенанс**: любой, кто может коммитить, может написать парсящийся файл. Контроль —
человеческий review audit-only коммита в PR плюс прогон предиката. Не описывайте эту
запись как доказательство того, что Codex действительно запускался.

Отдельно: арбитр прямо отметил, что не мог независимо подтвердить существование, размеры
и баннеры внешних stdout-журналов — они лежат вне выданного read-only дерева. Это
ограничение, а не пробел в записи.

Локальный build/test арбитр тоже не запускал: это записало бы артефакты в дерево и
нарушило строгий read-only. Проверка сборки — ниже, из основного worktree.

## Локальные проверки

`swift build --build-tests` и `swift test` на `11952cf` — зелёные, 56/56.

Смоук-тесты после правки [P2] реально **выполняются**, а не пропускаются
(`SMOKE engine unit device=128 systemDefaultInput=79`) — это и подтверждает, что
skip-путь больше не маскирует запрос к CoreAudio.

## Что осталось открытым и НЕ закрывается этим PR

- **#17 [P0]** — два неограниченных ожидания на главном потоке в штатном пути
  `stopRecording()`. Требует async teardown с карантином сессии, а не таймаута: после
  таймаута нельзя отдавать URL, иначе опоздавший писатель испортит файл, который уже
  читает транскрипция. Факт регистрации задачи приоритет не снижает.
- **#19 [P2]** — `inputDevices()`/`inputChannelCount()` глотают `OSStatus`; предшествует
  ветке.
- **#14, #15, #16** — дефекты, предшествующие ветке, ждут приоритизации.
- Живые аудио-сценарии (горячая замена устройства посреди фразы, старт сразу после
  смены, заклинивший callback) автотестом не проверяются: тест менял бы глобальную
  настройку ввода всей машины. Владелец подтвердил вручную только базовый случай —
  диктовку при активных наушниках.

VERDICT: SHIP
